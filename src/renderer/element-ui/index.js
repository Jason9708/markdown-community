import Vue from 'vue'
import 'element-ui/lib/theme-chalk/index.css'


import { Icon, Button, Input, Form, FormItem, Carousel, CarouselItem, Loading, Upload, Message } from 'element-ui'

Vue.use(Icon).use(Button).use(Input).use(Form).use(FormItem).use(Carousel).use(CarouselItem).use(Loading).use(Upload)