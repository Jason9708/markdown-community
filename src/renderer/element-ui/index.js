import Vue from 'vue'
import 'element-ui/lib/theme-chalk/index.css'


import { Icon, Button, Input, Form, FormItem } from 'element-ui'

Vue.use(Icon).use(Button).use(Icon).use(Input).use(Form).use(FormItem)